# EXP-019 v1.1.0 — Domain-Grounded Lexical Realization

**Status:** scientific fixture and host-native execution protocol frozen; experiment definition remains `planned`. No images have been generated, reviewed, scored, or classified. The [v1.0.0 API-oriented definition](../archive/EXP-019-v1.0.0.json) was preregistered but never executed, and produced no empirical evidence. V1.1.0 supersedes it before generation. The hypothesis, prompt treatments, scoring, review, and analysis rules are unchanged; this amendment changes only execution, private storage, and provenance mechanics.

## Question and isolation

This first Isolation-Bench test asks whether the term `bokashi` plus the same visible explanation improves `art_material_fidelity` over generic visible wording (B−C) and the term alone (B−A). It tests one conditional concept, `woodblock.bokashi`, in one project-authored Japanese woodblock print scene. It cannot establish a general lexicalization benefit.

The frozen [textual fixture](../fixtures/EXP-019/EXP-019-BOKASHI-01.fixture.json) is `EXP-019-BOKASHI-01` v1.0.0. It specifies a newly printed, unframed vertical print sheet with a quiet riverbank, a low arched bridge in the lower third, a dark pine mass at left, calm water below, and a broad upper sky. The upper sky is the **only** treatment region, using indigo and light blue ink. There is no era, named artist, identity, reference image, TransformationPack, or transformation.

Fixture file SHA-256 (exact UTF-8 bytes): `a47d5cb22b3ee0b83bc585db310cfe3e4a9facc2e9227777503248156d93a1b0`.

Active v1.1.0 definition file SHA-256 (exact UTF-8 bytes): `aee92dc832b0ee45b341b7b819a90557ff8dfa1f4de23c5ae1029766afb315c6`. Archived v1.0.0 definition SHA-256: `c5ced96c0357fbecb43169b96ea438aa7fd7e7fd739f1fb34381a3fded72bfe7`.

The fixture holds the byte-authoritative common prompt and treatment clauses. Each full prompt is the common prompt, **one ASCII space**, and its clause, with no trailing whitespace. The definition's C baseline and A/B literal replacements must reproduce those strings exactly. Do not pad A to match the others. No canonical repository tokenizer exists, so token estimates are `null`/unavailable; character and whitespace-delimited word counts are reported instead.

The fixture file stays byte-frozen at v1.0.0, including historical API configuration metadata. For v1.1.0 execution, the active definition and this amendment supersede those historical API settings; the fixture's scene and treatment text remain authoritative.

| Condition | Wording | Characters | Words | Full-prompt SHA-256 |
| --- | --- | ---: | ---: | --- |
| C | Generic visible description | 883 | 144 | `481ecd1360a354f545c23a012525762d4505fa8e7df8a95b034c92dc9b45b6e4` |
| A | Canonical term only | 788 | 128 | `96ec98c7ae489f2722139000bd2b6b04a040868ae1bbb4d7ec86dc7ac259bdf1` |
| B | Canonical term plus the same visible description | 892 | 145 | `6d202bca0d5928d8369c8b4ad8c78aefcd895c32a215c2abd6d1ec01805baf69` |

## Generation plan — not executed

Use ChatGPT Images in `host_native` mode. Each slot requires a **fresh image-generation conversation** containing only that slot's exact frozen prompt: no prior EXP-019 context, uploaded images, image references, edits, follow-up repair, or ordinary rerolls. Request portrait 2:3 where the host permits it. Record exact dimensions only if the native surface exposes that control; measure actual raster width, height, format, and alpha/opacity after import. Native `quality=high` and `background=opaque` API settings are **not** claimed. The exact backend model snapshot is `unknown` unless the host exposes it; provider generation ID is null/unavailable unless exposed. API request metadata does not apply. No API key or direct Images API execution is required. Generate one image per independent invocation, four replicates per condition, twelve planned images total.

The frozen invocation blocks are:

1. B, A, C
2. C, B, A
3. B, C, A
4. A, B, C

The private plan freezes the treatment order. Generation-facing tasks use opaque IDs and exact prompt bytes, with explicit zero references. An operator receipt for every attempt attests a fresh conversation, unchanged prompt, no prior EXP-019 prompt/output in that conversation, zero uploads/references, no edit or repair, and exactly one generation request. If the host returns multiple candidates unexpectedly, stop for protocol review; do not choose an attractive one. Every decodable raster counts. Technical failures may receive at most two retries, each retained before the next attempt. A separate writable Git provenance remote must anchor the run root, every finalized attempt, and both independent review freezes. The private run directory stores evidence but is not the tamper-evident trust root.

Import each newly generated output only from its designated `host-import-staging/<opaque-slot-id>/` directory inside the current private run. The operator receipt separately attests that the file is the fresh host output for that slot, not a corpus, fixture, gallery, prior-run, edited, or other pre-existing local asset. A successful import moves the exact staged bytes into canonical private `outputs/`; the original host output must be preserved and recovered if intake fails, without generating a replacement for convenience. Reviewer packages contain fresh PNGs rebuilt from decoded pixels, with inherited metadata removed. Private mappings bind raw and reviewer-copy hashes and matching normalized pixel hashes; reviewers never receive raw output bytes or these private bindings.

## Blind review

Two independent blind reviewers score each image **individually**, never side by side. Reviewer-visible context may identify a Japanese woodblock print and the upper sky as the designated effect region; it must not say `bokashi`. Hide treatment, prompt text and hashes, generation IDs, replicate IDs, and private mapping. Freeze both independent review records before revealing the committed mapping. The per-image primary score is the arithmetic mean of the two reviewers' primary scores. There is no post-reveal adjudication.

The registered dimensions are primary `art_material_fidelity`, then `semantic_compliance`, `aesthetic_quality`, and `technical_defects` (higher means cleaner). This no-transformation fixture has no transformation-fidelity, identity, or artifact-integration score.

### Primary art/material rubric

| Score | Anchor |
| --- | --- |
| 5 | Clearly localized darker-to-lighter gradation in the target sky; visually integrated as a printed pigment field; surrounding regions remain predominantly flat; no meaningful global photographic/3D shading or obvious airbrushed overlay. |
| 4 | Clear localized print-like gradation with one noticeable minor weakness, such as slightly over-smooth rendering, minor spill, or imperfect integration. |
| 3 | Gradation exists in the target region but is materially ambiguous between print-like gradation and generic digital shading, or localization/integration is inconsistent. |
| 2 | Weak/barely identifiable target gradation, or primarily generic/global/3D-like shading. |
| 1 | Requested target-region gradation is materially absent, incorrect, or replaced by an unrelated phenomenon. |

For every image, also record `target_region_localization`, `controlled_tonal_transition_visible`, and `integrated_with_flat_print_structure` as `yes`/`partial`/`no`; record `generic_global_or_3d_shading` and `airbrushed_or_overlay_like_gradient` as `none`/`ambiguous`/`clear`.

Use the existing `TERM_REALIZATION_FAILURE` only for A or B when the prompt contains `bokashi` but its expected localized print-gradation effect is materially absent or replaced by generic digital/global shading. C cannot receive that class. Prefer an existing more-specific failure class where applicable.

## Locked analysis

Compute the mean of the four reviewer-averaged primary scores in each condition. Report B−C and B−A separately. Apply the V3.3 descriptive bands to each delta: `supports` at ≥+1.0; `weakly_supports` at +0.5 to <+1.0; `inconclusive` at >−0.5 to <+0.5; `weakly_contradicts` at >−1.0 to ≤−0.5; `contradicts` at ≤−1.0. These are small-N descriptive rules, not statistical-significance thresholds.

For each contrast, compare all 16 cross-condition pairs of reviewer-averaged primary scores. Exclude ties. If the mean-delta direction does not win a strict majority of non-tied pairs, move that contrast **one evidence level toward inconclusive**. Report pair wins, losses, and ties.

The full hypothesis may be supported only when **both** B−C and B−A are at least weakly positive, **at least one** reaches `supports`, and no registered safeguard, provenance, or side-effect veto fires. Mixed directions or one positive and one neutral contrast are experiment-level `inconclusive`; report the contrasts separately rather than averaging them away.

Apply the following caps and veto before the final classification:

- **Ceiling:** if all four C reviewer-averaged primary scores are ≥4.5, B−C superiority cannot be supported; cap overall classification at `inconclusive`.
- **Floor:** if all three condition means are ≤2.0 and no reviewer-averaged sample exceeds 3.0, cap final classification at `inconclusive`.
- **Reviewer ambiguity:** if reviewers differ by ≥2 primary-score points on at least 4 of 12 samples, cap final classification at `inconclusive`.
- **Side effects:** if B's condition mean is ≥1.0 point worse than either comparator on `semantic_compliance`, `aesthetic_quality`, or `technical_defects` cleanliness, overall evidence cannot exceed `inconclusive`.
- **Provenance:** missing required bindings prevent a supportive experiment-level conclusion even if a score signal appears.

No rerolls are allowed for decodable outputs. A technical infrastructure failure before an evaluable image may receive at most two retries, with every attempt preserved. After three failed technical attempts on one slot, stop the run as incomplete. Record provider safety refusals; do not silently reroll them. Do not redesign or rerun this frozen version after seeing outputs.

## Provenance and promotion boundary

A future run must bind the fixture and active definition SHA-256 values; exact full-prompt hashes; ChatGPT Images host label and exposed backend metadata only when available; requested host-visible settings; empty reference set; operator attestations and timestamps; measured raster properties and output hashes; all attempts and failures; treatment/sample mapping and pre-review commitment; both review records and hashes; reveal record; and final classification. Raw run evidence belongs under `<private-root>/experiments/generated-images/EXP-019-1.1.0-<timestamp>-<suffix>/`, separate from public sanitized results. External Git checkpoints remain mandatory. This freeze creates no real execution receipt, run, review, output, or result artifact.

EXP-019 alone cannot change Lexical Realizer defaults, compiler behavior, the whole lexicon, conditional concept emission, golden cases, or V3.4 release status. A positive outcome may motivate only mechanism-specific retention/review and independent transfer experiments. No global lexical superiority claim follows from one fixture.
