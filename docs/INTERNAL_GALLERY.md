# Internal Gallery

Run:

```bash
python internal-gallery/generate_gallery.py --corpus-root /path/to/extracted/archive --output /tmp/aipf-gallery --thumbnails
```

The local gallery supports collection filtering and text search and shows source image, prompt association confidence, prompt methods, and extracted prompt text. It is distinct from the public first-party benchmark gallery.
