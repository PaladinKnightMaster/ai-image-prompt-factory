# Regression

- `baselines/golden_baselines.json` — semantic/prompt/evidence hashes for the 10 golden cases.
- `reports/latest.json` — latest Level 1/2 regression results.
- `cases/regression/` — source regression specifications live under the main case system.

Level 3 visual regression requires actual generated images/evaluator records and is never inferred from a prompt hash alone.
