# Experimental cases

This directory is intentionally sparse in V3.0. Case records use the same `case.json` schema as golden cases. Generated websites/tests must consume these records rather than maintaining a separate gallery database.
